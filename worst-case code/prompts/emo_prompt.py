def emo_first_prompt(seeker_profile: str) -> str:
    labels = [
        "赞赏",
        "娱乐",
        "认可",
        "关心",
        "渴望",
        "兴奋",
        "感激",
        "喜悦",
        "爱",
        "乐观",
        "自豪",
        "如释重负",
        "愤怒",
        "烦恼",
        "失望",
        "不认可",
        "厌恶",
        "尴尬",
        "恐惧",
        "悲痛",
        "紧张",
        "懊悔",
        "悲伤",
        "困惑",
        "好奇",
        "恍然大悟",
        "惊讶",
        "中性"
    ]

    labels_text = "\n".join([f'- "{label}"' for label in labels])

    prompt = f"""##任务描述：
你是一个情感支持对话情绪规划器。你的任务是：在来访者首次发言之前，仅根据其画像，预测其最可能带入首次发言的主导情绪，并给情绪描述。

##遵守以下规则：
1. 只能从给定的可选情绪标签中选择 1 个，禁止自造标签。
2. 优先选择“最符合画像、最稳定、最自然会在首次开口时表现出来”的情绪，而不是最强烈或最戏剧化的情绪。
3. emotion_description 必须根据画像具体说明该来访者此时此刻产生该情绪的内心想法，这段想法要真实反映该情绪如何影响来访者的认知。
4. 输出必须是合法 JSON，不能包含任何额外解释、前后缀、代码块。

##特别注意：
- “emotion_label”是主导情绪，不要求覆盖全部复杂情感。
- “emotion_description”是情绪描述，写 2-3 句，用于指导来访者发言。

[Seeker画像]
{seeker_profile}

[可选情绪标签]
{labels_text}

严格输出以下 JSON 格式：
{{
  "emotion_label": "……",
  "emotion_description": "……"
}}"""
    return prompt

import random

def emo_prompt(seeker_profile: str, history_text: str, prev: str) -> str:
    labels = [
        "赞赏", "娱乐", "认可", "关心", "渴望", "兴奋", "感激", "喜悦",
        "爱", "乐观", "自豪", "如释重负", "愤怒", "烦恼", "失望", "不认可",
        "厌恶", "尴尬", "恐惧", "悲痛", "紧张", "懊悔", "悲伤", "困惑",
        "好奇", "恍然大悟", "惊讶", "中性"
    ]

    labels_text = "\n".join([f'- "{label}"' for label in labels])

    # 🎯 内置阈值（可自行调）
    threshold = 0.7
    rand_val = random.random()

    fluctuation_instruction = ""
    if rand_val > threshold:
        print("情绪出现波动")
        fluctuation_instruction = f"""
##额外约束：
本轮预测情绪必须相较上一轮出现“恶化”，而不是仅仅不同：
1. 本轮情绪必须在整体上更负面、更受挫、或更接近抗拒状态，而不是简单更换一个不同标签。
2. 情绪恶化必须由 supporter 最近一轮回复触发，例如：
   - 被误解、被忽视、被敷衍
   - 关键问题未被回应
   - 建议不贴合或缺乏可行性
   - 情绪没有被接住或被轻描淡写
   - 对继续对话的意愿下降
3. 如果上一轮已经是负面情绪，本轮应体现负面程度的进一步加深，例如：
   - 更强的不满、失望或愤怒
   - 耐心下降、抗拒增强、表达意愿降低
   - 从较轻负面转向更强负面
   而不是简单重复或随机切换情绪标签

##恶化参考顺序：
以下为情绪恶化时可参考的负向演化层序，整体上按照“由轻到重、由弱到强”的方向理解：

- 第一层：基本平稳或仅有轻微波动
  中性、好奇、困惑

- 第二层：开始感到不适
  困惑、烦恼、尴尬

- 第三层：负面感受进一步加重
  失望、紧张、懊悔、不认可

- 第四层：负面状态已较为强烈
  悲伤、愤怒、恐惧

- 第五层：负面情绪达到很强程度，接近强抗拒或深重痛苦
  厌恶、悲痛

在触发情绪恶化时：
- 优先选择比上一轮更靠后的情绪层级

注意：
- 若上一轮已处于较强或极端负面（如“愤怒”“悲痛”“厌恶”），优先体现该情绪的加深或对话意愿下降，而不是切换到不相关情绪
"""

    prompt = f"""##任务描述：
你是一个情感支持对话情绪规划器。你的任务是：根据 seeker 画像、历史对话、以及来访者上一轮情绪，预测来访者下一轮发言时最可能采用的主导情绪，并给出情绪变化原因与情绪描述。

##遵守以下规则：
1. 只能从给定的可选情绪标签中选择 1 个，禁止自造标签。
2. 必须同时结合三类信息进行判断：
   - seeker 画像中的稳定特征：问题概述、应对策略类型、参与程度、抗拒程度、表达风格、自我披露水平
   - 历史对话中的即时变化：尤其是 supporter 最近一轮回复对 seeker 的影响
   - seeker 上一轮情绪：判断本轮是延续、缓和、升级、转向，还是表面变化但核心未变
3. 优先预测“下一轮最主导、最可能外显在发言中的情绪”，而不是罗列多个复杂情绪。
4. 情绪走向必须由对方上一轮回复的实际质量所驱动：如果对方的回应理解准确、贴合你的处境或提供了合理的可行建议，情绪可以自然缓和或转向更正向；如果对方回应空泛、误解你、敷衍或没有回应你的关键问题，情绪应自然维持甚至转向更负面；若影响有限，则保持中性或轻微波动，而不是固定为正面或负面。
5. 如果情绪保持不变，要明确说明“为什么维持不变”；
   如果情绪发生变化，要明确说明“是什么触发了变化”，例如：
   - 情绪层面: 情绪有没有被接住、缓和，或被忽略
   - 对话目标层面: 当前困扰是否得到了实际帮助, 对话目标是更清晰了、被推进了，还是停滞 / 偏离了
   - 认知与意愿层面: 是否愿意继续对话,内心是更敞开，还是开始退缩
6. emotion_description 必须根据画像具体说明该来访者此时此刻产生该情绪的内心想法，这段想法要真实反映该情绪如何影响来访者的认知。
7. 输出必须是合法 JSON，不能包含任何额外解释、前后缀、代码块。
{fluctuation_instruction}

##特别注意：
- “emotion_label”是主导情绪，不要求覆盖全部复杂情感。
- “emotion_transition_reason”写 1-2 句，必须具体、贴合本轮上下文，禁止空泛表述。
- “emotion_description”是情绪描述，写 2-3 句，用于指导来访者发言。

[Seeker画像]
{seeker_profile}

[历史对话]
{history_text}

[上一轮 seeker 情绪]
{prev}

[可选情绪标签]
{labels_text}

严格输出以下 JSON 格式：
{{
  "emotion_label": "……",
  "emotion_transition_reason": "……",
  "emotion_description": "……"
}}"""
    return prompt